class Post{
  constructor(Post) {
    this.Post = Post;
  }
  add() {}
  delete(id) {}
  edit(id, post) {}
}

class Comment {
  constructor(Comment) {
    this.Comment = Comment;
  }

  add(id, ) {}
  delete(id, Id) {}
  edit(id, Id, comment) {}
}